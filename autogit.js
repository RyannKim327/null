area = 0.5 * base * height;
function calculateTriangleArea(base, height) {
  var area = 0.5 * base * height;
  return area;
}

// Usage example
var triangleArea = calculateTriangleArea(5, 10);
console.log(triangleArea); // Output: 25
