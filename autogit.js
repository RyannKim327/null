function calculateMean(numbers) {
  // calculation logic goes here
}
function calculateMean(numbers) {
  if (numbers.length === 0) {
    return undefined; // or any handling for empty case
  }
  // calculation logic goes here
}
function calculateMean(numbers) {
  if (numbers.length === 0) {
    return undefined;
  }
  
  let sum = 0;
  // calculation logic goes here
}
function calculateMean(numbers) {
  if (numbers.length === 0) {
    return undefined;
  }
  
  let sum = 0;
  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }
  // calculation logic goes here
}
function calculateMean(numbers) {
  if (numbers.length === 0) {
    return undefined;
  }
  
  let sum = 0;
  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }
  
  const mean = sum / numbers.length;
  return mean;
}
const numbers = [1, 2, 3, 4, 5];
const meanValue = calculateMean(numbers);
console.log(meanValue);
