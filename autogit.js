import random

# Define the range [min_val, max_val]
min_val = 10
max_val = 20

# Generate a random number within the specified range
random_number = random.randint(min_val, max_val)

print(random_number)
