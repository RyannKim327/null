const myString = "Hello, World!";
const substring = "World";

if (myString.includes(substring)) {
  console.log("The string contains the specified substring.");
} else {
  console.log("The string does not contain the specified substring.");
}
